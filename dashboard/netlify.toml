# Deploy do dashboard (Vite/React) no Netlify.
[build]
  command = "npm ci && npm run build"
  publish = "dist"

# SPA: qualquer rota cai no index.html (client-side routing do react-router).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
